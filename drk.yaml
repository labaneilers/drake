dockerFile: Dockerfile.build
dockerDir: /code
commands:
  default: 
    command: echo default
  something:
    command: echo st
    dockerFile: Dockerfile.alt
  onhost:
    command: echo
    noDocker: true
  build:
    command: printenv
    dockerDir: /go/src/drake
    env:
      - TRAVIS_JOB_ID
  release:
    command: ./release.sh
    dockerDir: /go/src/drake
