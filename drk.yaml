dockerFile: Dockerfile.build
dockerDir: /code
commands:
  default: 
    command: echo default
  something:
    command: echo st
    dockerFile: Dockerfile.alt
  onhost:
    command: echo
    noDocker: true
  drkbuild:
    command: ./build.sh
    dockerDir: /go/src/drk
  release:
    command: ./release.sh
    dockerDir: /go/src/drk
