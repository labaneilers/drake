dockerFile: Dockerfile.build
dockerDir: /code
commands:
  default: 
    command: echo default
  something:
    command: echo st
    dockerFile: Dockerfile.alt
  onhost:
    command: echo
    noDocker: true
